const X72=require('../src/generator/x72Codegen');
const fs=require('fs');
const {buildNativeProgram}=require('../src/zlang/nativeCompiler');
const {registerizeProgram}=require('../src/zlang/registerVm');
const {buildEmissionPlan}=require('../src/zlang/emitter');
const source="local TeleportService = game:GetService(\"TeleportService\")\nlocal HttpService     = game:GetService(\"HttpService\")\nlocal Players         = game:GetService(\"Players\")\nlocal LocalPlayer     = Players.LocalPlayer\n\nlocal PlaceId = 107778070777162\n\nlocal WindUI = loadstring(game:HttpGet(\"https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua\"))()\n\nlocal HopEngine = {\n    VisitedServers   = {},\n    Blacklisted      = {},\n    IsSearching      = false,\n    TotalScanned     = 0,\n    TargetMin        = 1,\n    TargetMax        = 3,\n    AutoRetryOnFail  = false,\n    AntiHackerActive = false,\n}\n\nfunction HopEngine:ResetBlacklist()\n    self.VisitedServers = {}\n    self.Blacklisted    = {}\n    self.TotalScanned   = 0\n    self.IsSearching    = false\n    WindUI:Notify({ Title = \"🧹 Memoria Limpia\", Content = \"Historial de servidores reseteado.\", Duration = 2 })\nend\n\nfunction HopEngine:ScanAndFindServer(minP, maxP, maxPagesToScan)\n    minP = minP or self.TargetMin\n    maxP = maxP or self.TargetMax\n    maxPagesToScan = maxPagesToScan or 6\n\n    local cursor = \"\"\n    local pageCount = 0\n    local candidates = {}\n\n    while pageCount < maxPagesToScan do\n        pageCount = pageCount + 1\n\n        local endpoint = string.format(\n            \"https://games.roblox.com/v1/games/%s/servers/0?sortOrder=Asc&limit=100\",\n            tostring(PlaceId)\n        )\n        if cursor ~= \"\" then\n            endpoint = endpoint .. \"&cursor=\" .. cursor\n        end\n\n        local success, rawData = pcall(function()\n            return game:HttpGet(endpoint)\n        end)\n\n        if not success or not rawData then break end\n\n        local decodeSuccess, parsed = pcall(function()\n            return HttpService:JSONDecode(rawData)\n        end)\n\n        if not decodeSuccess or not parsed or not parsed.data then break end\n\n        for _, server in ipairs(parsed.data) do\n            self.TotalScanned = self.TotalScanned + 1\n            local sId      = server.id\n            local sPlaying  = server.playing\n            local sMax      = server.maxPlayers\n\n            local isNotCurrent  = (sId ~= game.JobId)\n            local isNotVisited  = not self.VisitedServers[sId]\n            local isNotBlack    = not self.Blacklisted[sId]\n            local isNotGlitched = (sPlaying > 0)\n            local isInRange     = (sPlaying >= minP and sPlaying <= maxP)\n            local hasSpace      = (sPlaying < sMax)\n\n            if isNotCurrent and isNotVisited and isNotBlack and isNotGlitched and isInRange and hasSpace then\n                table.insert(candidates, {\n                    id      = sId,\n                    playing = sPlaying,\n                    ping    = server.ping or 999,\n                })\n            end\n        end\n\n        cursor = parsed.nextPageCursor or \"\"\n        if cursor == \"\" then break end\n        task.wait(0.1)\n    end\n\n    return candidates\nend\n\nfunction HopEngine:VerifyServerLive(serverId, minP, maxP)\n    local endpoint = string.format(\n        \"https://games.roblox.com/v1/games/%s/servers/0?sortOrder=Asc&limit=100\",\n        tostring(PlaceId)\n    )\n\n    local success, rawData = pcall(function()\n        return game:HttpGet(endpoint)\n    end)\n\n    if success and rawData then\n        local decodeSuccess, parsed = pcall(function()\n            return HttpService:JSONDecode(rawData)\n        end)\n\n        if decodeSuccess and parsed and parsed.data then\n            for _, server in ipairs(parsed.data) do\n                if server.id == serverId then\n                    if server.playing >= minP and server.playing <= maxP then\n                        return true, server.playing\n                    else\n                        return false, server.playing\n                    end\n                end\n            end\n        end\n    end\n\n    return true, minP\nend\n\nfunction HopEngine:ExecuteHop(minP, maxP)\n    if self.IsSearching then\n        self.IsSearching = false\n    end\n\n    self.IsSearching = true\n    self.TargetMin   = minP\n    self.TargetMax   = maxP\n\n    task.delay(8, function()\n        if self.IsSearching then\n            self.IsSearching = false\n        end\n    end)\n\n    WindUI:Notify({\n        Title    = \"🧠 ANALIZANDO SERVIDORES\",\n        Content  = string.format(\"Buscando y verificando salas de [%d-%d] personas...\", minP, maxP),\n        Duration = 2,\n    })\n\n    task.spawn(function()\n        local candidates = self:ScanAndFindServer(minP, maxP, 6)\n\n        if #candidates > 0 then\n            math.randomseed(os.time() + tick())\n\n            local serverValidated = false\n            local targetServer = nil\n\n            while #candidates > 0 and not serverValidated do\n                local index = math.random(1, #candidates)\n                local selected = candidates[index]\n                \n                WindUI:Notify({\n                    Title    = \"🔍 PRE-VERIFICANDO\",\n                    Content  = \"Analizando servidor objetivo en tiempo real...\",\n                    Duration = 1.5,\n                })\n\n                local isValid, currentPlayers = self:VerifyServerLive(selected.id, minP, maxP)\n\n                if isValid then\n                    serverValidated = true\n                    targetServer = selected\n                    targetServer.playing = currentPlayers\n                else\n                    self.Blacklisted[selected.id] = true\n                    table.remove(candidates, index)\n                    \n                    WindUI:Notify({\n                        Title    = \"⚠️ SALA DESCARTADA\",\n                        Content  = string.format(\"Detectadas %d personas (Fuera de limite). Buscando otra...\", currentPlayers),\n                        Duration = 2,\n                    })\n                    task.wait(0.3)\n                end\n            end\n\n            if serverValidated and targetServer then\n                self.VisitedServers[targetServer.id] = true\n\n                WindUI:Notify({\n                    Title    = \"✅ SERVIDOR VERIFICADO\",\n                    Content  = string.format(\"Conectando a sala limpia con %d persona(s)...\", targetServer.playing),\n                    Duration = 3,\n                })\n\n                task.wait(0.3)\n\n                local teleSuccess = pcall(function()\n                    TeleportService:TeleportToPlaceInstance(PlaceId, targetServer.id, LocalPlayer)\n                end)\n\n                task.wait(1.5)\n                self.IsSearching = false\n\n                if not teleSuccess then\n                    self.Blacklisted[targetServer.id] = true\n                    if self.AutoRetryOnFail then\n                        task.wait(0.5)\n                        self:ExecuteHop(minP, maxP)\n                    end\n                end\n            else\n                self.IsSearching = false\n                WindUI:Notify({\n                    Title    = \"🔍 Servidores Inestables\",\n                    Content  = \"Las salas encontradas se llenaron rápido. Reintentando...\",\n                    Duration = 3,\n                })\n                if self.AutoRetryOnFail then\n                    task.wait(1)\n                    self:ExecuteHop(minP, maxP)\n                end\n            end\n        else\n            self.IsSearching = false\n            WindUI:Notify({\n                Title    = \"🔍 Sin salas exactas\",\n                Content  = string.format(\"No hay salas de %d-%d jugadores disponibles.\", minP, maxP),\n                Duration = 3,\n            })\n        end\n    end)\nend\n\nTeleportService.TeleportInitFailed:Connect(function(player)\n    if player == LocalPlayer then\n        HopEngine.IsSearching = false\n        WindUI:Notify({\n            Title    = \"🚨 Error de Roblox\",\n            Content  = \"Fallo al conectar. Liberando motor...\",\n            Duration = 2,\n        })\n    end\nend)\n\ntask.spawn(function()\n    while task.wait(3) do\n        if HopEngine.AntiHackerActive and not HopEngine.IsSearching then\n            for _, player in ipairs(Players:GetPlayers()) do\n                if player ~= LocalPlayer and player.Character then\n                    local hrp = player.Character:FindFirstChild(\"HumanoidRootPart\")\n                    if hrp and hrp.AssemblyLinearVelocity.Magnitude > 85 then\n                        HopEngine.Blacklisted[game.JobId] = true\n                        WindUI:Notify({\n                            Title    = \"🚨 Hacker/Bot Detectado\",\n                            Content  = \"Jugador volando. Ejecutando salida limpia...\",\n                            Duration = 3,\n                        })\n                        task.wait(0.5)\n                        HopEngine:ExecuteHop(1, 3)\n                        break\n                    end\n                end\n            end\n        end\n    end\nend)\n\n-- =========================================================\n-- Z NEXUS // NEO GLASS UI\n-- Solo se cambia la presentación. Las lógicas/callbacks\n-- del motor están conservadas.\n-- =========================================================\n\nWindUI:AddTheme({\n    Name = \"ZNexusNeo\",\n    Accent = Color3.fromHex(\"#D94CFF\"),\n    Background = Color3.fromHex(\"#09070D\"),\n    Outline = Color3.fromHex(\"#7E5A91\"),\n    Text = Color3.fromHex(\"#F7EEFF\"),\n    Placeholder = Color3.fromHex(\"#9B8AA3\"),\n    Button = Color3.fromHex(\"#21152B\"),\n    Icon = Color3.fromHex(\"#E58AFF\"),\n})\n\nWindUI:SetTheme(\"ZNexusNeo\")\nWindUI:SetFont(\"rbxasset://fonts/families/Montserrat.json\")\nWindUI:SetNotificationLower(true)\n\nlocal Window = WindUI:CreateWindow({\n    Title                       = \"Z NEXUS\",\n    Icon                        = \"scan-eye\",\n    Author                      = \"STEAL AN EGG  //  COMMAND CENTER\",\n    Folder                      = \"StealAnEgg_Nexus\",\n    Size                        = UDim2.fromOffset(650, 450),\n    MinSize                     = Vector2.new(570, 380),\n    MaxSize                     = Vector2.new(900, 650),\n    Transparent                 = true,\n    Theme                       = \"ZNexusNeo\",\n    Resizable                   = true,\n    SideBarWidth                = 170,\n    HideSearchBar               = true,\n    ScrollBarEnabled            = false,\n    Background                  = \"https://raw.githubusercontent.com/z22026we-beep/prueba/heads/main/VeniceAI_D7aZBZN.png\",\n    BackgroundImageTransparency = 0.20,\n\n    OpenButton                  = {\n        Title      = \"Z NEXUS\",\n        Enabled    = true,\n        OnlyMobile = false,\n        Draggable  = true,\n    },\n})\n\npcall(function()\n    WindUI:ToggleAcrylic(true)\nend)\n\n-- =========================================================\n-- TABS\n-- =========================================================\nlocal HomeTab   = Window:Tab({ Title = \"Dashboard\", Icon = \"layout-dashboard\" })\nlocal HopTab    = Window:Tab({ Title = \"Server Hop\", Icon = \"radar\" })\nlocal ConfigTab = Window:Tab({ Title = \"System\", Icon = \"sliders-horizontal\" })\nlocal SocialTab = Window:Tab({ Title = \"Network\", Icon = \"radio\" })\n\n-- =========================================================\n-- DASHBOARD\n-- =========================================================\nHomeTab:Section({\n    Title = \"NEXUS // OVERVIEW\",\n    Icon = \"sparkles\",\n})\n\nHomeTab:Paragraph({\n    Title     = \"SERVER HOP ENGINE\",\n    Desc      = \"Motor listo. Escanea salas, verifica jugadores y conecta automáticamente.\",\n    Color     = \"Purple\",\n    Image     = \"https://cdn.discordapp.com/embed/avatars/1.png\",\n    ImageSize = 42,\n    Buttons   = {\n        {\n            Title = \"SCAN 1\",\n            Icon = \"scan\",\n            Callback = function()\n                HopEngine:ExecuteHop(1, 1)\n            end,\n        },\n        {\n            Title = \"SCAN 2-3\",\n            Icon = \"radar\",\n            Callback = function()\n                HopEngine:ExecuteHop(2, 3)\n            end,\n        },\n    },\n})\n\nHomeTab:Space()\n\nHomeTab:Section({\n    Title = \"LIVE STATUS\",\n    Icon = \"activity\",\n})\n\nHomeTab:Paragraph({\n    Title = \"ENGINE ONLINE\",\n    Desc  = \"Anti-Hacker: \" .. (HopEngine.AntiHackerActive and \"ACTIVE\" or \"STANDBY\")\n        .. \"   •   Auto Retry: \" .. (HopEngine.AutoRetryOnFail and \"ACTIVE\" or \"STANDBY\"),\n    Color = \"Blue\",\n    ImageSize = 0,\n})\n\nHomeTab:Space()\n\nHomeTab:Paragraph({\n    Title = \"MEMORY\",\n    Desc  = \"Servers scanned: \" .. tostring(HopEngine.TotalScanned)\n        .. \"   •   Searching: \" .. (HopEngine.IsSearching and \"YES\" or \"NO\"),\n    Color = \"Grey\",\n    ImageSize = 0,\n    Buttons = {\n        {\n            Title = \"RESET\",\n            Icon = \"refresh-cw\",\n            Callback = function()\n                HopEngine:ResetBlacklist()\n            end,\n        },\n    },\n})\n\n-- =========================================================\n-- SERVER HOP\n-- =========================================================\nHopTab:Section({\n    Title = \"SERVER SCANNER\",\n    Icon = \"scan-search\",\n})\n\nHopTab:Paragraph({\n    Title = \"TARGET  /  SOLO\",\n    Desc  = \"Encuentra una sala con exactamente 1 jugador y verifica antes de entrar.\",\n    Color = \"Purple\",\n    ImageSize = 0,\n    Buttons = {\n        {\n            Title = \"EXECUTE\",\n            Icon = \"zap\",\n            Callback = function()\n                HopEngine:ExecuteHop(1, 1)\n            end,\n        },\n    },\n})\n\nHopTab:Space()\n\nHopTab:Paragraph({\n    Title = \"TARGET  /  2 — 3\",\n    Desc  = \"Busca una sala pequeña entre 2 y 3 jugadores.\",\n    Color = \"Blue\",\n    ImageSize = 0,\n    Buttons = {\n        {\n            Title = \"EXECUTE\",\n            Icon = \"zap\",\n            Callback = function()\n                HopEngine:ExecuteHop(2, 3)\n            end,\n        },\n    },\n})\n\nHopTab:Space()\n\nHopTab:Section({\n    Title = \"ENGINE CONTROLS\",\n    Icon = \"cpu\",\n})\n\nHopTab:Button({\n    Title = \"FORCE UNLOCK\",\n    Desc  = \"Libera el motor si una búsqueda se queda congelada.\",\n    Icon  = \"unlock\",\n    Callback = function()\n        HopEngine.IsSearching = false\n        WindUI:Notify({\n            Title = \"🔓 ENGINE UNLOCKED\",\n            Content = \"Puedes ejecutar otra búsqueda.\",\n            Duration = 2\n        })\n    end,\n})\n\nHopTab:Button({\n    Title = \"CLEAR MEMORY\",\n    Desc  = \"Borra servidores visitados y descartados.\",\n    Icon  = \"trash-2\",\n    Callback = function()\n        HopEngine:ResetBlacklist()\n        WindUI:Notify({\n            Title = \"🧹 MEMORY CLEARED\",\n            Content = \"Historial de servidores reseteado.\",\n            Duration = 2\n        })\n    end,\n})\n\n-- =========================================================\n-- SYSTEM / SETTINGS\n-- =========================================================\nConfigTab:Section({\n    Title = \"PROTECTION\",\n    Icon = \"shield-check\",\n})\n\nConfigTab:Toggle({\n    Title = \"Anti-Hackers / Bots\",\n    Desc  = \"Detecta movimiento anormal y ejecuta una salida limpia.\",\n    Icon  = \"shield-alert\",\n    Type  = \"Checkbox\",\n    Value = false,\n    Callback = function(val)\n        HopEngine.AntiHackerActive = val\n    end\n})\n\nConfigTab:Toggle({\n    Title = \"Auto-Retry\",\n    Desc  = \"Vuelve a buscar automáticamente si el teleport falla.\",\n    Icon  = \"rotate-cw\",\n    Type  = \"Checkbox\",\n    Value = false,\n    Callback = function(val)\n        HopEngine.AutoRetryOnFail = val\n    end\n})\n\nConfigTab:Space()\n\nConfigTab:Section({\n    Title = \"APPEARANCE\",\n    Icon = \"palette\",\n})\n\nConfigTab:Dropdown({\n    Title  = \"Accent Theme\",\n    Desc   = \"Cambia el acento visual del panel.\",\n    Values = { \"Dark\", \"Midnight\", \"Bloom\", \"Rose\", \"Sea\", \"Sky\", \"Amber\", \"ZNexusNeo\" },\n    Value  = \"ZNexusNeo\",\n    Callback = function(tema)\n        WindUI:SetTheme(tema)\n    end,\n})\n\nConfigTab:Dropdown({\n    Title  = \"Interface Font\",\n    Desc   = \"Selecciona la tipografía de la interfaz.\",\n    Values = { \"BuilderSans\", \"Montserrat\", \"Nunito\", \"Oswald\", \"PermanentMarker\", \"LuckiestGuy\" },\n    Value  = \"Montserrat\",\n    Callback = function(fuente)\n        WindUI:SetFont(\"rbxasset://fonts/families/\" .. fuente .. \".json\")\n    end,\n})\n\n-- =========================================================\n-- NETWORK / SOCIAL\n-- =========================================================\nSocialTab:Section({\n    Title = \"Z NEXUS // NETWORK\",\n    Icon = \"radio-tower\",\n})\n\nSocialTab:Paragraph({\n    Title     = \"DISCORD\",\n    Desc      = \"Soporte, actualizaciones y comunidad.\",\n    Color     = \"Purple\",\n    Image     = \"https://cdn.discordapp.com/embed/avatars/1.png\",\n    ImageSize = 42,\n    Buttons   = {\n        {\n            Title = \"COPY\",\n            Icon = \"clipboard-copy\",\n            Callback = function()\n                setclipboard(\"https://discord.gg/eaqCMKuFU\")\n                WindUI:Notify({\n                    Title = \"LINK COPIED\",\n                    Content = \"Discord copiado al portapapeles.\",\n                    Duration = 2\n                })\n            end,\n        },\n    },\n})\n\nSocialTab:Space()\n\nSocialTab:Section({\n    Title = \"CREATORS\",\n    Icon = \"users-round\",\n})\n\nSocialTab:Button({\n    Title = \"@nexus1_h\",\n    Desc  = \"TikTok profile\",\n    Icon  = \"at-sign\",\n    Callback = function()\n        setclipboard(\"https://www.tiktok.com/@nexus1_h\")\n        WindUI:Notify({ Title = \"COPIED\", Content = \"Perfil copiado.\", Duration = 2 })\n    end,\n})\n\nSocialTab:Button({\n    Title = \"@znoreste\",\n    Desc  = \"TikTok profile\",\n    Icon  = \"at-sign\",\n    Callback = function()\n        setclipboard(\"https://www.tiktok.com/@znoreste\")\n        WindUI:Notify({ Title = \"COPIED\", Content = \"Perfil copiado.\", Duration = 2 })\n    end,\n})\n\nprint(\"Z Nexus // Neo Glass UI loaded\")";
console.log('BENCH lines',source.split(/\r?\n/).length,'chars',source.length);
for(const optimize of [false,true]){
  try{
    const p=buildNativeProgram(source,{fallback:false,optimize,polymorphic:false});
    console.log('opt',optimize,'nativeFns',p.functions.length,'const',p.constants.length,'ins',p.functions.reduce((n,f)=>n+f.code.length,0));
    try{
      const r=registerizeProgram(JSON.parse(JSON.stringify(p)));
      console.log('  registerize OK','ins',r.functions.reduce((n,f)=>n+f.code.length,0));
    }catch(e){console.log('  registerize ERROR',e.message)}
    try{
      const e=buildEmissionPlan(p,{backend:'register'});
      console.log('  emitter OK','ins',e.functions.reduce((n,f)=>n+f.code.length,0));
    }catch(e){console.log('  emitter ERROR',e.message)}
  }catch(e){console.log('opt',optimize,'NATIVE ERROR',e.stack||e.message)}
}


const hard=require('../src/zlang/x72Hardening');
const {validateIR}=require('../src/zlang/ir');
const native2=buildNativeProgram(source,{fallback:false,optimize:true,polymorphic:false});
const stages=[
 ['base',()=>{}],
 ['strings',()=>hard.splitStringConstants(native2,{chance:0.28})],
 ['numbers',()=>hard.splitNumericConstants(native2,{chance:0.22})],
 ['opaque',()=>hard.injectOpaqueGuards(native2,{minFunctionLength:24})],
 ['distributed',()=>hard.injectDistributedOpaqueGuards(native2,{minFunctionLength:192,interval:192})],
 ['decoys',()=>hard.injectDecoyFunctions(native2,{count:0})],
 ['compact',()=>{}]
];
for(const [name,pass] of stages){
 try{pass(); validateIR(native2); const ins=native2.functions.reduce((n,f)=>n+f.code.length,0); const e=buildEmissionPlan(native2,{backend:'register'}); console.log('STAGE',name,'ins',ins,'emitIns',e.functions.reduce((n,f)=>n+f.code.length,0),'OK');}
 catch(e){console.log('STAGE',name,'ERROR',e.message);break;}
}
